import 'package:flutter/widgets.dart';

class LoginAnonymousPage extends StatelessWidget {
  const LoginAnonymousPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
